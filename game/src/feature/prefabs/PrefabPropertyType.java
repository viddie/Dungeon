package feature.prefabs;

/** Property types supported by prefab definitions and the level editor. */
public enum PrefabPropertyType {
  STRING,
  INTEGER,
  FLOAT,
  NUMBER_SLIDER,
  BOOLEAN,
  ENUM,
  POINT,
  REGION,
  VECTOR2,
  COLOR
}
